spring:
  datasource:
    url: jdbc:postgresql://db:5432/vesselpro
    username: vesselpro
    password: vesselpro
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8011